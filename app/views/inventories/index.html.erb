<% content_for(:title) do %>
  Flattrack | Inventory for <%= @flat.address %>
<% end %>

<div class="container">
  <div class="d-flex justify-content-between">
    <span>
      <h2 class="header">Inventory</h2>
    </span>
    <button style="margin-top:14px;" type="button" class="button-dark">
       <i class="fas fa-arrow-left"></i> &nbsp; Back
        </button>
  </div>
  <h3>Bedroom (<%= @bedroom.count %>)</h3>
  <div class="row">
    <% @bedroom.each do |inventory| %>
      <div class="col-lg-6 col-sm-12">
        <div class="card-inventory" style="background-image: linear-gradient(to right, rgba(0,0,0,0.9) 0%,rgba(255,255,255,0) 50%,rgba(0,0,0,0.9) 100%), url('<%= cl_image_path inventory.photo, height: 150, width: 400, crop: :fill %>')">
          <div class="d-flex flex-grow-1 justify-content-between">
            <span class="flex-grow-1">
              <h3 class=""><%= inventory.description.upcase %></h3>
            </span>
            <span>
              <h3 class="price"><%= inventory.historic_value %>€</h3>
            </span>
          </div>
          <div class="d-flex justify-content-between">
            <!-- <div class="d-flex flex-column"> -->
              <span class="mr-2">
                <p><%= inventory.name.capitalize %></p>
              </span>
            <!-- </div> -->
            <%= link_to "Replace", flat_inventory_path(@flat, inventory), method: :delete, class: "button-light" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
  <h3>Living room (<%= @living.count %>)</h3>
  <div class="row">
    <% @living.each do |inventory| %>
      <div class="col-lg-6 col-sm-12">
        <div class="card-inventory" style="background-image: linear-gradient(to right, rgba(0,0,0,0.9) 0%,rgba(255,255,255,0) 50%,rgba(0,0,0,0.9) 100%), url('<%= cl_image_path inventory.photo, height: 150, width: 400, crop: :fill %>')">
          <div class="d-flex flex-grow-1 justify-content-between">
            <span class="flex-grow-1">
              <h3 class=""><%= inventory.description.upcase %></h3>
            </span>
            <span>
              <h3 class="price"><%= inventory.historic_value %>€</h3>
            </span>
          </div>
          <div class="d-flex justify-content-between">
            <!-- <div class="d-flex flex-column"> -->
              <span class="mr-2">
                <p><%= inventory.name.capitalize %></p>
              </span>
            <!-- </div> -->
            <%= link_to "Replace", flat_inventory_path(@flat, inventory), method: :delete, class: "button-light" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
  <h3>Kitchen (<%= @kitchen.count %>)</h3>
  <div class="row">
    <% @kitchen.each do |inventory| %>
      <div class="col-lg-6 col-sm-12">
        <div class="card-inventory" style="background-image: linear-gradient(to right, rgba(0,0,0,0.9) 0%,rgba(255,255,255,0) 50%,rgba(0,0,0,0.9) 100%), url('<%= cl_image_path inventory.photo, height: 150, width: 400, crop: :fill %>')">
          <div class="d-flex flex-grow-1 justify-content-between">
            <span class="flex-grow-1">
              <h3 class=""><%= inventory.description.upcase %></h3>
            </span>
            <span>
              <h3 class="price"><%= inventory.historic_value %>€</h3>
            </span>
          </div>
          <div class="d-flex justify-content-between">
            <!-- <div class="d-flex flex-column"> -->
              <span class="mr-2">
                <p><%= inventory.name.capitalize %></p>
              </span>
            <!-- </div> -->
            <%= link_to "Replace", flat_inventory_path(@flat, inventory), method: :delete, class: "button-light" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
  <h3>Bathroom (<%= @bathroom.count %>)</h3>
  <div class="row">
    <% @bathroom.each do |inventory| %>
      <div class="col-lg-6 col-sm-12">
        <div class="card-inventory" style="background-image: linear-gradient(to right, rgba(0,0,0,0.9) 0%,rgba(255,255,255,0) 50%,rgba(0,0,0,0.9) 100%), url('<%= cl_image_path inventory.photo, height: 150, width: 400, crop: :fill %>')">
          <div class="d-flex flex-grow-1 justify-content-between">
            <span class="flex-grow-1">
              <h3 class=""><%= inventory.description.upcase %></h3>
            </span>
            <span>
              <h3 class="price"><%= inventory.historic_value %>€</h3>
            </span>
          </div>
          <div class="d-flex justify-content-between">
            <!-- <div class="d-flex flex-column"> -->
              <span class="mr-2">
                <p><%= inventory.name.capitalize %></p>
              </span>
            <!-- </div> -->
            <%= link_to "Replace", flat_inventory_path(@flat, inventory), method: :delete, class: "button-light" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
